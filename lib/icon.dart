import 'package:flutter/material.dart';

class MyIcon extends StatelessWidget {
  @override
  Icon build(BuildContext context) {
    return Icon(Icons.android, color: Color.fromARGB(255, 255, 255, 0));
  }
}
